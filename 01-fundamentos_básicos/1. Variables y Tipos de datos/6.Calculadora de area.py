# Calcula el área de un rectángulo
# Pide al usuario base y altura
# Calcula y muestra el área

base = float(input("Base: "))
altura = float(input("Altura: "))

print(f"El área de un rectángulo es: {base*altura}")
print(f"El perímetro de un rectángulo es: {2*base + 2*altura}")